<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="haconglinh1990.redmineandroid">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_voz2"
        android:label="MeoMeo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.authenticate.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.authenticate.RegisterActivity"
            android:label="@string/title_activity_register"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.authenticate.ForgotPasswordActivity"
            android:label="@string/title_activity_forgot_pass_word"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.ultils.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.project.ProjectActivity" />
        <activity android:name=".activities.issue.IssueDetailActivity" />
        <activity android:name=".activities.issue.CreateIssueActivity" />
        <activity android:name=".activities.issue.EditIssueActivity"></activity>
    </application>

</manifest>